/**
 * Thin wrapper around MUI Button.
 * Re-exported here so consumers import from a consistent path.
 * Add shared variant presets here as the design system grows.
 */

export { Button } from '@mui/material';
