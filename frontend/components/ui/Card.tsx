/**
 * Thin wrapper around MUI Card.
 * Theme overrides in lib/theme/index.ts apply hover lift + border automatically.
 */

export { Card, CardContent, CardActions, CardMedia, CardHeader } from '@mui/material';
